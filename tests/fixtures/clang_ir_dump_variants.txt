remark: unrelated toolchain output
***** IR Dump After SimplifyCFGPass on function (helper) *****
define void @helper() {
entry:
  ret void
}
*** IR Dump After GlobalDCEPass on [module] ***
; ModuleID = 'variant.c'
define void @helper() {
entry:
  ret void
}
*** IR Dump After MissingScope ***
define void @must_not_parse() {
  ret void
}
*** IR Dump After EmptyPass on helper ***

*** IR Dump Before BoundaryPass on helper ***
define void @before_only() {
  ret void
}
