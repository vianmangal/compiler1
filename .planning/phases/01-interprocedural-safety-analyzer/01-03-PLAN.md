---
phase: 01-interprocedural-safety-analyzer
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - looplift/report.py
  - looplift/cli.py
  - looplift/__main__.py
  - examples/safe_map.c
  - examples/unsafe_global.c
  - examples/unsafe_transitive.c
  - examples/recursive_helper.c
  - tests/test_reporting.py
  - tests/test_cli.py
  - tests/test_integration.py
  - README.md
autonomous: true
requirements:
  - INPT-01
  - INPT-02
  - INPT-03
  - RPRT-01
  - RPRT-02
  - QUAL-01
must_haves:
  truths:
    - "One analyze command runs frontend, program analysis, transitive summarization, classification, and reporting end to end (D-10)."
    - "analysis.json and report.md expose functions, edges, loops, effects, decisions, reason codes, evidence, source and Clang metadata."
    - "Examples visibly demonstrate safe, direct-unsafe, transitive-unsafe, and recursive decisions (D-11)."
---

<objective>
Expose the completed analyzer as a faculty-friendly CLI with deterministic JSON/Markdown reports, representative examples, documentation, and end-to-end verification.
</objective>

## Artifacts this phase produces

- `looplift.report`: `write_reports`, `result_to_dict`, `render_markdown`.
- `looplift.cli`: `build_parser`, `run_analysis`, `main`.
- `looplift.__main__`: module entry point.
- CLI: `looplift analyze SOURCE [--clang PATH] [--output DIR]`.
- Examples and tests listed in frontmatter.

<tasks>

<task type="auto">
  <name>Task 1: Write deterministic JSON and Markdown reports</name>
  <read_first>
    <file>looplift/model.py</file>
    <file>looplift/classifier.py</file>
    <file>.planning/phases/01-interprocedural-safety-analyzer/01-CONTEXT.md</file>
  </read_first>
  <files>looplift/report.py, tests/test_reporting.py</files>
  <action>Serialize the full AnalysisResult into `analysis.json` with lowercase enum values and stable ordering. Render `report.md` with tool/source metadata, function effects, call edges, and one loop section per decision showing status, reason codes, evidence, and call chain. Write atomically into an explicit output directory and refuse to replace existing files unless the command opts in.</action>
  <verify><automated>python3 -m unittest tests.test_reporting -v</automated></verify>
  <acceptance_criteria>
    - Report tests verify both files, deterministic repeated output, all required top-level JSON fields, and nonempty evidence for every non-safe loop.
    - Markdown includes headings for Functions, Call Graph, and Loop Decisions plus visible safe/unsafe/unsupported labels.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire CLI, examples, docs, and integration tests</name>
  <read_first>
    <file>looplift/clang_frontend.py</file>
    <file>looplift/analyzer.py</file>
    <file>looplift/interprocedural.py</file>
    <file>looplift/classifier.py</file>
    <file>looplift/report.py</file>
    <file>docs/PRD.md</file>
  </read_first>
  <files>looplift/cli.py, looplift/__main__.py, examples/safe_map.c, examples/unsafe_global.c, examples/unsafe_transitive.c, examples/recursive_helper.c, tests/test_cli.py, tests/test_integration.py, README.md</files>
  <action>Implement argparse subcommand `analyze` with source, `--clang`, `--output`, and `--force`. Orchestrate the full pipeline and print counts plus the report paths. Convert expected frontend/report failures into concise stderr and non-zero exit codes. Add the four named examples and README sections for problem choice, architecture, supported subset, installation, demo, output, limitations, and tests. Add CLI unit tests and a conditional real-Clang integration test that validates the safe and transitive examples.</action>
  <verify><automated>python3 -m unittest discover -s tests -v &amp;&amp; python3 -m looplift analyze examples/safe_map.c --output build/safe-map --force</automated></verify>
  <acceptance_criteria>
    - `python3 -m looplift analyze examples/safe_map.c --output build/safe-map --force` exits 0 and writes both reports.
    - The safe example has at least one safe loop; direct, transitive, and recursive examples are not approved.
    - Terminal output states analyzed loop count, status counts, and exact report paths.
    - The full standard-library test suite exits 0; real-Clang tests skip only when Clang is unavailable.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

- Report paths are explicit pathlib targets; no source-derived path traversal is used.
- Existing report files are not overwritten without `--force`.
- Markdown escapes or fences source-controlled evidence so generated data cannot become executable instructions.

</threat_model>

<verification>
Run the full test suite and analyze all four examples with local Clang. Confirm generated JSON parses and every example's decision matches README claims.
</verification>

<success_criteria>
RPRT-01,RPRT-02 and QUAL-01 are met; a first-time user can reproduce the Phase 1 demo with one documented command.
</success_criteria>
