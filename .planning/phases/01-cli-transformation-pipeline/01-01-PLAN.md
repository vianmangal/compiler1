---
phase: 01-cli-transformation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iris_analyzer/__init__.py
  - iris_analyzer/model.py
  - iris_analyzer/parser.py
  - iris_analyzer/compiler.py
  - tests/fixtures/clang_ir_dump.txt
  - tests/fixtures/clang_ir_dump_variants.txt
  - tests/test_parser.py
  - tests/test_compiler.py
autonomous: true
requirements:
  - INPUT-02
  - INPUT-03
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-04
  - QUAL-02
must_haves:
  truths:
    - "IR Dump After sections become ordered typed snapshots carrying pass name, scope, and complete IR body."
    - "Consecutive unchanged IR is suppressed independently per scope, while changed states remain globally ordered and bounded by a positive cap."
    - "The installed or explicitly selected Clang is resolved to an executable and invoked through the real O1 print-after-all pipeline without running compiled code."
    - "Parser and compiler failures are represented as bounded errors that the dependent CLI plan can render without a traceback."
  artifacts:
    - path: "iris_analyzer/model.py"
      provides: "Frozen configuration, compiler-output, snapshot, and report-path contracts"
      exports: ["IRSnapshot", "CompilerOutput", "CLIConfig", "ReportPaths"]
    - path: "iris_analyzer/parser.py"
      provides: "LLVM IR dump parsing and per-scope change filtering"
      exports: ["parse_ir_dumps", "retain_changed", "normalize_ir"]
    - path: "iris_analyzer/compiler.py"
      provides: "Clang discovery and safe real-pipeline execution"
      exports: ["resolve_clang", "run_optimization_pipeline"]
    - path: "tests/test_parser.py"
      provides: "Fixture-driven parsing and filtering verification"
    - path: "tests/test_compiler.py"
      provides: "Clang discovery, subprocess-contract, timeout, and failure verification"
  key_links:
    - from: "iris_analyzer/parser.py"
      to: "iris_analyzer/model.py"
      via: "parse_ir_dumps constructs IRSnapshot values"
      pattern: "IRSnapshot"
    - from: "iris_analyzer/compiler.py"
      to: "clang"
      via: "subprocess.run argument list with shell=False and -O1 -mllvm -print-after-all"
      pattern: "print-after-all"
    - from: "iris_analyzer/compiler.py"
      to: "iris_analyzer/model.py"
      via: "successful capture returns CompilerOutput with the resolved command and stderr dump"
      pattern: "CompilerOutput"
---

<objective>
Build the capture foundation for Phase 1: typed contracts, deterministic LLVM dump parsing and per-scope change filtering, plus safe discovery and invocation of a real local Clang O1 pipeline.

Purpose: Isolate and prove the version-variable compiler boundary before the dependent CLI/reporting slice turns it into the one-command user workflow.
Output: A Python 3.10+ standard-library model/parser/compiler foundation with representative fixtures and focused automated tests.
</objective>

<execution_context>
@/Users/vian/.codex/gsd-core/workflows/execute-plan.md
@/Users/vian/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-cli-transformation-pipeline/01-CONTEXT.md
@docs/PRD.md
@docs/IMPLEMENTATION_PLAN.md

There is no existing application code, codebase map, phase research, prior phase summary, external runtime dependency, or package-manager install to preserve. The current on-disk artifacts define IRis/P01 as authoritative even if a planning helper exposes stale metadata from an interrupted and reverted project pivot.
</context>

## Artifacts this phase produces

This plan owns the eight-file capture-foundation share of the Phase 1 artifacts; Plan 01-02 owns the CLI, reporting, example, documentation, and integration-test share.

| File | Contract produced | Required fields, functions, or behavior |
|------|-------------------|------------------------------------------|
| iris_analyzer/__init__.py | Package marker | Makes iris_analyzer importable without third-party packaging machinery |
| iris_analyzer/model.py | Frozen IRSnapshot dataclass | order: int, pass_name: str, scope: str, ir: str |
| iris_analyzer/model.py | Frozen CompilerOutput dataclass | clang_path: pathlib.Path, command: tuple[str, ...], dump_text: str |
| iris_analyzer/model.py | Frozen CLIConfig dataclass | source: pathlib.Path, clang_override: str or None, output_dir: pathlib.Path, max_snapshots: int |
| iris_analyzer/model.py | Frozen ReportPaths dataclass | root: pathlib.Path, manifest: pathlib.Path, timeline: pathlib.Path, snapshots_dir: pathlib.Path |
| iris_analyzer/parser.py | Parser and filter functions | parse_ir_dumps(dump_text), retain_changed(snapshots, max_snapshots), normalize_ir(ir) |
| iris_analyzer/compiler.py | Compiler boundary | resolve_clang(override), run_optimization_pipeline(source, clang_path); uses -O1 -S -emit-llvm -mllvm -print-after-all and never runs the compiled program |
| tests/fixtures/*.txt | Representative LLVM stderr | Function/module banners, interleaved scopes, repeated IR, irrelevant diagnostics, malformed sections, and banner variants |
| tests/test_parser.py | Parser/filter contract tests | Exact ordering, normalization, per-scope baseline, change retention, cap, and truncation assertions |
| tests/test_compiler.py | Compiler-boundary tests | PATH/override discovery, exact safe subprocess invocation, timeout, and failure mapping assertions |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed snapshot contracts and prove LLVM dump parsing</name>
  <requirements>CAPT-02, CAPT-03, CAPT-04, QUAL-02</requirements>
  <files>iris_analyzer/__init__.py, iris_analyzer/model.py, iris_analyzer/parser.py, tests/fixtures/clang_ir_dump.txt, tests/fixtures/clang_ir_dump_variants.txt, tests/test_parser.py</files>
  <read_first>
    - .planning/phases/01-cli-transformation-pipeline/01-CONTEXT.md
    - docs/PRD.md
    - docs/IMPLEMENTATION_PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - A well-formed IR Dump After banner becomes one IRSnapshot with the original stream order, pass name, scope, and complete IR body.
    - Function and module banner variants parse without treating unrelated diagnostics or IR Dump Before text as after-pass snapshots.
    - Interleaved scopes keep independent prior IR values: identical IR is suppressed only against the preceding snapshot for the same scope.
    - The first observed state for each scope seeds comparison without being emitted as a changed transformation; subsequent identical normalized states are omitted and changed states are retained.
    - CRLF versus LF and trailing line whitespace normalize consistently, while semantic IR text changes remain visible.
    - max_snapshots must be positive, caps the globally ordered retained list after filtering, and sets truncated only when another changed snapshot exists beyond the cap.
  </behavior>
  <action>Create the package marker and the four frozen dataclasses specified in Artifacts this phase produces, then implement an isolated line-oriented parser for IR Dump After sections and a deterministic per-scope filter. Use anchored banner recognition that extracts pass and scope without absorbing surrounding asterisks; ignore malformed or bodyless banners rather than emitting corrupt snapshots. Preserve stream order and readable IR text. Implement normalization and the per-scope last-seen map explicitly per D-05 and D-06, and enforce the positive retained cap per D-07. Use only Python 3.10+ standard-library types per D-11. Write fixture-driven unittest cases before implementation and include function/module variants, interleaving, repetition, malformed sections, and cap truncation.</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p 'test_parser.py' -v</automated>
  </verify>
  <acceptance_criteria>
    - The parser fixture produces ordered snapshots whose pass_name, scope, and ir values match asserted literals.
    - The interleaved-scope test fails if filtering compares globally instead of independently per scope.
    - Unchanged same-scope bodies are absent, changed bodies remain ordered, and the retained list never exceeds max_snapshots.
    - No third-party imports or dependency metadata are added.
  </acceptance_criteria>
  <done>CAPT-02, CAPT-03, CAPT-04, and their QUAL-02 unit-test coverage have deterministic core behavior, including the explicit first-state baseline and truncation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and test the safe real-Clang capture boundary</name>
  <requirements>INPUT-02, INPUT-03, CAPT-01, QUAL-02</requirements>
  <files>iris_analyzer/compiler.py, tests/test_compiler.py</files>
  <read_first>
    - iris_analyzer/model.py
    - .planning/phases/01-cli-transformation-pipeline/01-CONTEXT.md
    - docs/PRD.md
    - docs/IMPLEMENTATION_PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - resolve_clang discovers clang through shutil.which when no override is supplied and resolves an explicit executable path or command-name override when supplied.
    - Missing, non-file, or non-executable compiler candidates become bounded domain errors suitable for a concise non-zero CLI diagnostic.
    - run_optimization_pipeline invokes the resolved compiler with an argument list containing -O1, -S, -emit-llvm, -mllvm, and -print-after-all; shell is false, output goes to os.devnull, and stderr is captured as the dump stream.
    - Successful execution returns CompilerOutput containing the resolved Clang path, exact command tuple, and stderr dump.
    - A non-zero result and the 30-second subprocess timeout become bounded domain errors and no code path runs a compiled program.
  </behavior>
  <action>Implement resolve_clang for default PATH discovery and the explicit override per D-02, requiring a resolved executable regular file and preserving a readable error boundary for missing/unusable compilers or compilation failures per D-03. Implement run_optimization_pipeline around subprocess.run using the exact real optimization pipeline required by D-04; resolve the source to an absolute path before passing it as an argument, use shell=False, capture text stderr, set a 30-second timeout for the scoped small-program workflow, compile LLVM output to os.devnull, and never execute a produced program per D-12. Return CompilerOutput with the resolved compiler path, exact command tuple, and stderr dump. Use unittest.mock to assert both resolution paths, exact invocation, success capture, timeout, and non-zero failure mapping. Keep runtime and tests standard-library-only per D-11.</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p 'test_compiler.py' -v</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert the exact optimization flags, argument-list execution, shell=False, stderr capture, os.devnull output, and timeout.
    - PATH discovery and explicit executable override both resolve to recorded paths; missing and non-executable candidates fail through the bounded error contract.
    - Non-zero compiler exit and timeout are distinguishable, readable failures that the dependent CLI can map without a traceback.
    - No code path compiles and then runs the source program, and no third-party dependency is added.
  </acceptance_criteria>
  <done>INPUT-02, INPUT-03, CAPT-01, and their QUAL-02 compiler-boundary coverage are ready for Plan 01-02 to expose through the command.</done>
</task>

</tasks>

<dependency_graph>

| Task | Needs | Creates | Checkpoint |
|------|-------|---------|------------|
| 1 | Current Phase 1 context and canonical product requirements | Dataclass contracts, parser, per-scope filter, fixtures, and unit tests | No |
| 2 | Task 1 model contracts | Clang discovery/capture boundary and isolated boundary tests | No |

Execution is sequential inside this plan: Task 1 -> Task 2. Plan 01-02 depends on the contracts, parser, filter, and compiler boundary produced here.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IRis -> Clang subprocess | Python arguments cross into an external executable resolved from PATH or --clang. |
| Clang stderr -> parser/model | Toolchain-controlled, version-variable, potentially large text becomes structured in-memory snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | iris_analyzer/compiler.py | mitigate | Per D-12, call subprocess.run with an explicit argument list and shell=False, resolve the source path, write compiler output to os.devnull, never execute the compiled program, and assert these properties in tests/test_compiler.py. |
| T-01-02 | Spoofing | PATH/--clang resolution | mitigate | Per D-02, resolve with shutil.which, require an executable regular file, and return the resolved path and exact command for later manifest disclosure. |
| T-01-03 | Denial of Service | Clang execution and dump retention | mitigate | Apply a 30-second subprocess timeout, parse only IR Dump After sections, filter unchanged same-scope states, and enforce the positive retained cap per D-07. |
| T-01-04 | Information Disclosure | compiler diagnostics | accept | This is an explicitly local educational CLI operating on user-selected files; bounded local diagnostic text is required for D-03 and is not transmitted. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | There are no package-manager installs; runtime and tests use Python standard library only per D-11. If execution proposes any package, stop and run the package-legitimacy gate before installation. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

This phase-set audit records the exact ownership split; it intentionally includes Plan 01-02 rows so no source item is lost by the scope-only revision.

| Source | ID | Feature/requirement | Plan task | Status | Notes |
|--------|----|---------------------|-----------|--------|-------|
| GOAL | — | One command turns a local C file into a trustworthy changed-pass timeline | 01-01 Tasks 1-2; 01-02 Tasks 1-3 | COVERED | Capture foundation feeds the dependent CLI/report slice and smoke proof |
| REQ | INPUT-01 | Analyze one existing C source | 01-02 Task 2 | COVERED | Exact positional-source validation |
| REQ | INPUT-02 | Clear source/toolchain errors | 01-01 Task 2; 01-02 Task 2 | COVERED | Bounded compiler errors rendered as concise CLI failures |
| REQ | INPUT-03 | Explicit Clang override | 01-01 Task 2; 01-02 Task 2 | COVERED | Resolver plus --clang exposure |
| REQ | CAPT-01 | Real LLVM O1 pipeline | 01-01 Task 2; 01-02 Task 3 | COVERED | Exact subprocess contract plus real-Clang smoke proof |
| REQ | CAPT-02 | Ordered pass, scope, and IR body | 01-01 Task 1 | COVERED | Typed parser fixture assertions |
| REQ | CAPT-03 | Per-scope changed-only snapshots | 01-01 Task 1 | COVERED | Interleaved-scope filtering tests |
| REQ | CAPT-04 | Positive retained cap | 01-01 Task 1; 01-02 Task 2 | COVERED | Filter cap/truncated contract and CLI exposure |
| REQ | RPRT-01 | JSON manifest | 01-02 Task 1 | COVERED | Stable explicit schema |
| REQ | RPRT-02 | Markdown timeline | 01-02 Task 1 | COVERED | Ordered readable entries and links |
| REQ | RPRT-03 | Numbered LLVM files | 01-02 Task 1 | COVERED | Stable sanitized filenames |
| REQ | RPRT-04 | Terminal summary | 01-02 Task 2 | COVERED | Retained count and report path |
| REQ | QUAL-01 | README example flow | 01-02 Task 3 | COVERED | Exact first-run command and example |
| REQ | QUAL-02 | Automated parser/filter/report/CLI/Clang verification | 01-01 Tasks 1-2; 01-02 Tasks 1-3 | COVERED | Focused tests and conditional real-Clang integration |
| RESEARCH | — | No RESEARCH.md exists; research is disabled for this standard-library phase | — | EXCLUDED | No research-derived scope to audit |
| CONTEXT | D-01 | Exactly one existing .c source | 01-02 Task 2 | COVERED | Cited in action |
| CONTEXT | D-02 | PATH clang plus --clang override | 01-01 Task 2; 01-02 Task 2 | COVERED | Cited in both resolver and CLI actions |
| CONTEXT | D-03 | Readable non-zero errors | 01-01 Task 2; 01-02 Task 2 | COVERED | Cited at boundary and presentation layers |
| CONTEXT | D-04 | Real -O1 -mllvm -print-after-all | 01-01 Task 2; 01-02 Task 2 | COVERED | Cited in capture and orchestration actions |
| CONTEXT | D-05 | Parse pass, scope, and IR body | 01-01 Task 1; 01-02 Task 2 | COVERED | Cited in parser and wiring actions |
| CONTEXT | D-06 | Compare independently by scope | 01-01 Task 1; 01-02 Task 2 | COVERED | Cited in filter and wiring actions |
| CONTEXT | D-07 | Positive --max-snapshots | 01-01 Task 1; 01-02 Task 2 | COVERED | Filter enforcement and CLI exposure |
| CONTEXT | D-08 | Manifest, timeline, numbered .ll output | 01-02 Task 1 | COVERED | Cited in reporting action |
| CONTEXT | D-09 | Retained count and report location | 01-02 Task 2 | COVERED | Cited in CLI action |
| CONTEXT | D-10 | Stable sanitized names and structured JSON | 01-02 Task 1 | COVERED | Cited in reporting action |
| CONTEXT | D-11 | Python 3.10+ standard library | 01-01 Tasks 1-2; 01-02 Tasks 1-3 | COVERED | Cited in every implementation/testing action |
| CONTEXT | D-12 | Argument list, shell false, never run program | 01-01 Task 2; 01-02 Task 2 | COVERED | Cited and directly asserted |
| CONTEXT | D-13 | Tests, smoke path, example, README | 01-02 Task 3 | COVERED | Cited in action |
</source_audit>

<verification>
1. Run python3 -m unittest discover -s tests -p 'test_parser.py' -v; all parser, normalization, per-scope filtering, cap, and truncation cases pass.
2. Run python3 -m unittest discover -s tests -p 'test_compiler.py' -v; all PATH/override discovery, exact subprocess, timeout, non-zero exit, and non-execution cases pass.
3. Run python3 -m compileall -q iris_analyzer tests; all files created by this plan compile under Python 3.10+ syntax.
4. Inspect imports in iris_analyzer and tests; no third-party runtime or test dependency, package metadata, or install step exists.
</verification>

<success_criteria>
- CAPT-01 through CAPT-04 have executable, focused automated coverage before CLI/report wiring begins.
- The parser preserves ordered pass/scope/IR data, compares independently per scope, and reports truncation only when changed output exists beyond the cap.
- The compiler boundary resolves PATH and explicit Clang selections, runs the exact real O1 print-after-all pipeline with shell=False, captures stderr, and never runs compiled code.
- Plan 01-01 modifies exactly eight files and leaves all CLI, reporting, example, README, and integration-test ownership to dependent Plan 01-02.
- No Phase 2/3 explanation, diff, filter UI, browser UI, or unrelated P05/LoopLift scope is introduced.
</success_criteria>

<output>
Create .planning/phases/01-cli-transformation-pipeline/01-01-SUMMARY.md when done.
</output>
