---
phase: 01-cli-transformation-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - iris_analyzer/compiler.py
  - tests/test_compiler.py
  - tests/test_integration.py
  - README.md
autonomous: true
gap_closure: true
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - RPRT-03
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "Every numbered .ll snapshot produced by the analyze command is a complete standalone LLVM module accepted by the same resolved Clang that generated it."
    - "The real O1 capture command retains -print-changed and adds -mllvm -print-module-scope without weakening timeout, byte-limit, shell-free, or no-execution safeguards."
    - "Focused command-contract tests fail if either changed-pass or module-scope instrumentation is removed or reordered from the expected safe argument tuple."
    - "The local-Clang integration test validates every generated snapshot and reports the specific artifact and compiler diagnostic when any snapshot is invalid."
  artifacts:
    - path: "iris_analyzer/compiler.py"
      provides: "Real Clang O1 capture command that emits changed-pass dumps as complete module-scope LLVM IR"
      contains: "-print-module-scope"
    - path: "tests/test_compiler.py"
      provides: "Focused exact-command regression coverage for changed-pass plus module-scope instrumentation"
      contains: "-print-module-scope"
    - path: "tests/test_integration.py"
      provides: "Real-toolchain proof that every persisted numbered LLVM snapshot parses independently"
    - path: "README.md"
      provides: "Accurate Clang instrumentation compatibility and standalone-snapshot behavior"
  key_links:
    - from: "iris_analyzer/compiler.py"
      to: "clang"
      via: "shell-free argument tuple containing -O1, -print-changed, and -print-module-scope"
      pattern: "print-changed|print-module-scope"
    - from: "tests/test_compiler.py"
      to: "iris_analyzer/compiler.py"
      via: "exact expected subprocess command assertion"
      pattern: "expected_command"
    - from: "tests/test_integration.py"
      to: "manifest.json clang and snapshots entries"
      via: "invoke the manifest's resolved Clang with -x ir -S -emit-llvm for every generated .ll file"
      pattern: "manifest\[\"clang\"\]|-x"
---

<objective>
Close the Phase 1 verification gap by making every persisted numbered LLVM snapshot a complete standalone module and proving that property against the selected real Clang.

Purpose: Restore the report's trustworthy `.ll` artifact contract without changing the CLI shape, report schema, retained-event semantics, or simple standard-library architecture.
Output: Module-scope Clang capture, exact command regression coverage, per-snapshot real-Clang validation, and narrowly updated compatibility documentation.
</objective>

<execution_context>
@/Users/vian/.codex/gsd-core/workflows/execute-plan.md
@/Users/vian/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-cli-transformation-pipeline/01-CONTEXT.md
@.planning/phases/01-cli-transformation-pipeline/01-01-SUMMARY.md
@.planning/phases/01-cli-transformation-pipeline/01-02-SUMMARY.md
@.planning/phases/01-cli-transformation-pipeline/01-VERIFICATION.md
@iris_analyzer/compiler.py
@tests/test_compiler.py
@tests/test_integration.py
@README.md

The authoritative verification isolated one deterministic failure: function-pass dumps produced without LLVM's module-scope print option omit module attributes or metadata, so 12 of 21 generated `.ll` artifacts failed independent Clang parsing. An isolated verifier probe confirmed that adding `-mllvm -print-module-scope` makes all 21 parse successfully. The established implementation intentionally uses `-print-changed` to expose only LLVM-classified changes; this plan preserves that reviewed behavior and changes nothing outside the verified standalone-module gap.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit complete module-scope changed-pass dumps</name>
  <requirements>CAPT-01, CAPT-02, CAPT-03, QUAL-02</requirements>
  <files>iris_analyzer/compiler.py, tests/test_compiler.py</files>
  <read_first>
    - .planning/phases/01-cli-transformation-pipeline/01-VERIFICATION.md
    - iris_analyzer/compiler.py
    - tests/test_compiler.py
    - .planning/phases/01-cli-transformation-pipeline/01-CONTEXT.md
    - .planning/phases/01-cli-transformation-pipeline/01-02-SUMMARY.md
  </read_first>
  <behavior>
    - The exact real-Clang command is CLANG -O1 -S -emit-llvm -mllvm -print-changed -mllvm -print-module-scope SOURCE -o DEVNULL.
    - The capture still returns CompilerOutput with the resolved Clang path, exact command tuple, and bounded dump text.
    - The command still uses an argument list with shell=False, writes compiler output to os.devnull, applies the 30-second timeout and 16 MiB dump limit, and never executes compiled code.
    - Existing readable handling for unsupported changed-pass instrumentation, non-zero compiler exits, launch failure, timeout, and oversized output remains passing.
  </behavior>
  <action>First update the exact expected command in `tests/test_compiler.py` so the focused test fails without module-scope instrumentation. Then add a second `-mllvm`, `-print-module-scope` pair immediately after the retained `-mllvm`, `-print-changed` pair in `run_optimization_pipeline`. Preserve the current real `-O1` pipeline required by D-04 and the reviewed LLVM `-print-changed` semantics; do not restore `-print-after-all` or add a second filtering path. Preserve the bounded temporary-file capture, `_PIPELINE_TIMEOUT_SECONDS`, `_MAX_DUMP_BYTES`, `shell=False`, `stdout=DEVNULL`, resolved absolute paths, and never-run guarantee required by D-12. Use Python standard library only per D-11 and make no report-schema or parser changes because LLVM module-scope output resolves the payload defect at its source.</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p 'test_compiler.py' -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_compiler.py` asserts the complete exact command tuple, including both `-mllvm -print-changed` and `-mllvm -print-module-scope`.
    - `run_optimization_pipeline` passes that tuple directly to `subprocess.Popen` with `shell=False`, `stdout=subprocess.DEVNULL`, and bounded stderr capture.
    - All existing compiler discovery, failure, timeout, output-limit, and unsupported-instrumentation tests still pass.
    - No parser, reporting, CLI contract, dependency metadata, or third-party import is introduced.
  </acceptance_criteria>
  <done>The real capture boundary requests complete module IR for every LLVM-classified changed-pass event while retaining all established Phase 1 safety and compatibility behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every generated snapshot parses with the selected Clang</name>
  <requirements>RPRT-03, QUAL-01, QUAL-02</requirements>
  <files>tests/test_integration.py, README.md</files>
  <read_first>
    - .planning/phases/01-cli-transformation-pipeline/01-VERIFICATION.md
    - tests/test_integration.py
    - iris_analyzer/compiler.py
    - iris_analyzer/reporting.py
    - README.md
    - .planning/phases/01-cli-transformation-pipeline/01-02-SUMMARY.md
  </read_first>
  <behavior>
    - After the documented example succeeds, every path in `manifest["snapshots"]` is passed back to `manifest["clang"]` as LLVM IR and must parse with exit code zero.
    - Each validation command uses an argument list equivalent to CLANG -x ir -S -emit-llvm SNAPSHOT -o DEVNULL, applies a timeout, captures diagnostics, and never runs generated code.
    - A single invalid snapshot fails the integration test with its path and bounded Clang diagnostic, rather than allowing the report to pass on file existence alone.
    - The manifest capture command still contains both `-print-changed` and `-print-module-scope`, and pass/scope identities still exactly match the independently parsed changed-event stream.
  </behavior>
  <action>Strengthen `test_documented_example_generates_a_complete_report` so, after each manifest snapshot file is located and checked for banner leakage, the test invokes the same resolved compiler recorded in `manifest["clang"]` with `-x ir -S -emit-llvm`, the snapshot path, and `-o os.devnull`. Use `subprocess.run` with an explicit argument list, captured text output, `check=False`, and the existing 30-second integration timeout; assert zero for every file and include the snapshot path plus compiler stderr in the failure message. Do not execute any emitted program. Also assert the manifest command contains `-print-module-scope` while retaining the existing `-print-changed` and identity-order assertions. Update only the README requirement/report-layout sentences needed to state that compatible Clang must support the LLVM changed/module-scope instrumentation and that each numbered `.ll` is a complete independently parseable module; do not restructure unrelated documentation. This locks in D-08's numbered artifact contract and D-13's real-Clang proof using only the Python standard library per D-11.</action>
  <verify>
    <automated>python3 -m unittest discover -s tests -p 'test_integration.py' -v</automated>
  </verify>
  <acceptance_criteria>
    - When local Clang is available, the checked-in example generates at least two snapshots and every generated numbered `.ll` passes the same resolved Clang's LLVM IR parse command.
    - The test fails on the first invalid snapshot with the artifact path and a useful compiler diagnostic.
    - The raw changed-event identities and manifest snapshot identities remain equal and ordered, proving module-scope output did not alter retained event attribution.
    - README changes are limited to the newly verified compatibility and standalone-module behavior.
    - The validation subprocess is shell-free, bounded by timeout, writes output to `os.devnull`, and never runs compiled code.
  </acceptance_criteria>
  <done>Automated real-toolchain coverage proves every published `.ll` artifact is a complete standalone module, closing the sole verification gap.</done>
</task>

</tasks>

<dependency_graph>

| Task | Needs | Creates | Checkpoint |
|------|-------|---------|------------|
| 1 | Existing safe Clang boundary and verified remediation direction | Module-scope changed-pass command plus exact focused regression coverage | No |
| 2 | Task 1 module-scope capture | Per-artifact real-Clang validity proof and precise compatibility documentation | No |

Execution is sequential inside this Wave 3 gap plan: Task 1 -> Task 2. The plan depends on 01-02 because it strengthens the completed vertical CLI/report slice without changing its public inputs or report schema.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IRis -> selected Clang | Local source and LLVM instrumentation flags cross into the resolved external compiler through a shell-free argument tuple. |
| Clang dump -> persisted snapshots | Toolchain-controlled text becomes user-visible numbered `.ll` artifacts and must be complete, bounded, and attributable. |
| Integration test -> generated `.ll` files | Each persisted artifact is re-read by the same resolved compiler solely for safe LLVM IR parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | `iris_analyzer/compiler.py` command construction | mitigate | Assert the exact `-O1 -mllvm -print-changed -mllvm -print-module-scope` argument sequence in `tests/test_compiler.py`; pass it directly with `shell=False` per D-12. |
| T-01-11 | Spoofing | Snapshot compatibility validation | mitigate | Parse every artifact with the resolved Clang path recorded by the same run in `manifest["clang"]`, avoiding an unrelated compiler selected later from PATH. |
| T-01-12 | Denial of Service | Compiler capture and per-snapshot validation | mitigate | Preserve the 30-second/16 MiB production bounds; cap report snapshots; apply a 30-second timeout to each integration validation command. |
| T-01-13 | Elevation of Privilege | Snapshot validation subprocess | mitigate | Use a fixed argument list, no shell, `-S -emit-llvm`, and `-o os.devnull`; never link or execute source or generated code. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No installs are present or permitted; production and tests remain Python standard-library-only per D-11. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | One command produces a trustworthy timeline of LLVM passes that changed IR | 01-01, 01-02, 01-03 | COVERED | 01-03 closes the only verified trust gap by proving all published IR artifacts are complete modules. |
| REQ | INPUT-01 | Analyze one existing C source | 01-02 | COVERED | Existing CLI behavior remains unchanged. |
| REQ | INPUT-02 | Clear invalid-source/toolchain errors | 01-01, 01-02 | COVERED | Existing error tests remain required to pass. |
| REQ | INPUT-03 | Explicit Clang override | 01-01, 01-02 | COVERED | 01-03 validates artifacts with the resolved compiler recorded by either selection path. |
| REQ | CAPT-01 | Run the real LLVM O1 pipeline | 01-01, 01-02, 01-03 Task 1 | COVERED | Module-scope printing is added to the established real O1 changed-event command. |
| REQ | CAPT-02 | Ordered pass, scope, and IR body | 01-01, 01-03 Tasks 1-2 | COVERED | Bodies become complete modules without changing pass/scope order; integration compares identities. |
| REQ | CAPT-03 | Retain only changed consecutive same-scope snapshots | 01-01, 01-02, 01-03 Tasks 1-2 | COVERED | `-print-changed` and identity equality are explicitly preserved. |
| REQ | CAPT-04 | Positive snapshot cap | 01-01, 01-02 | COVERED | Cap behavior is unchanged and full-suite verification remains required. |
| REQ | RPRT-01 | JSON manifest | 01-02 | COVERED | Schema remains unchanged. |
| REQ | RPRT-02 | Markdown timeline | 01-02 | COVERED | Timeline remains unchanged. |
| REQ | RPRT-03 | Numbered `.ll` snapshots | 01-02, 01-03 Task 2 | COVERED | Every numbered file is now required to parse independently as LLVM IR. |
| REQ | RPRT-04 | Terminal summary | 01-02 | COVERED | Existing summary remains unchanged. |
| REQ | QUAL-01 | README example flow | 01-02, 01-03 Task 2 | COVERED | Compatibility and standalone-module behavior are documented narrowly. |
| REQ | QUAL-02 | Automated parser/filter/report/CLI/Clang verification | 01-01, 01-02, 01-03 Tasks 1-2 | COVERED | Exact command and every generated artifact gain focused regression coverage. |
| RESEARCH | — | No phase RESEARCH.md exists | — | EXCLUDED | Gap direction was confirmed directly by the authoritative verifier probe. |
| CONTEXT | D-01 | Exactly one existing `.c` source | 01-02 | COVERED | Unchanged. |
| CONTEXT | D-02 | PATH Clang plus explicit override | 01-01, 01-02, 01-03 Task 2 | COVERED | Same resolved manifest compiler validates snapshots. |
| CONTEXT | D-03 | Readable non-zero errors | 01-01, 01-02 | COVERED | Existing error suite remains passing. |
| CONTEXT | D-04 | Real Clang O1 LLVM pipeline | 01-01, 01-02, 01-03 Task 1 | COVERED | Current reviewed `-print-changed` instrumentation is retained and augmented with module scope. |
| CONTEXT | D-05 | Parse ordered pass, scope, and body | 01-01, 01-03 Task 2 | COVERED | Identity order remains independently checked. |
| CONTEXT | D-06 | Compare consecutive snapshots per scope | 01-01, 01-02 | COVERED | No filtering change. |
| CONTEXT | D-07 | Positive retained cap | 01-01, 01-02 | COVERED | No cap change. |
| CONTEXT | D-08 | Manifest, timeline, numbered `.ll` artifacts | 01-02, 01-03 Task 2 | COVERED | Standalone validity becomes an executable artifact invariant. |
| CONTEXT | D-09 | Retained count and report location | 01-02 | COVERED | Unchanged. |
| CONTEXT | D-10 | Stable sanitized names and structured JSON | 01-02 | COVERED | Paths and schema are unchanged. |
| CONTEXT | D-11 | Python 3.10+ standard library | 01-01, 01-02, 01-03 Tasks 1-2 | COVERED | No dependency or install is added. |
| CONTEXT | D-12 | Argument list, shell false, never run program | 01-01, 01-03 Tasks 1-2 | COVERED | Production and validation subprocesses preserve the safe non-executing boundary. |
| CONTEXT | D-13 | Tests, real-Clang proof, example, README | 01-02, 01-03 Task 2 | COVERED | Real integration now parses every artifact and docs state the verified contract. |
</source_audit>

<verification>
1. Run `python3 -m unittest discover -s tests -p 'test_compiler.py' -v`; the exact command includes `-print-changed` and `-print-module-scope`, and all capture safety/error tests pass.
2. Run `python3 -m unittest discover -s tests -p 'test_integration.py' -v`; when Clang is available, every numbered `.ll` artifact from the documented example is independently accepted by the manifest's resolved compiler.
3. Run `python3 -m unittest discover -s tests -v`; all parser, compiler, reporting, CLI, and integration regressions pass.
4. Run `python3 -m compileall -q iris_analyzer tests`; all Python files compile with the Phase 1 Python 3.10+ constraint.
5. Inspect imports and dependency metadata; no third-party package or install step exists.
</verification>

<success_criteria>
- The previously failing standalone-snapshot truth is reproducibly green: every numbered `.ll` produced from `examples/loop.c` parses with the same resolved Clang.
- The capture command contains both `-print-changed` and `-print-module-scope`, retains `-O1`, and still writes only to `os.devnull` without executing compiled code.
- Compiler command tests and the local-Clang integration test fail if module-scope instrumentation or per-artifact validation is removed.
- Manifest schema, timeline format, CLI inputs, per-scope changed-only semantics, cap behavior, and user-file safety remain unchanged.
- The full standard-library suite and compile check pass with no new dependency.
</success_criteria>

<output>
Create `.planning/phases/01-cli-transformation-pipeline/01-03-SUMMARY.md` when done.
</output>
